import React from 'react'
import { Typography, Avatar, Grid } from '@mui/material'

export default function chatMessage({avatar, message, side, }) {
  return (
        <>
            
        </>
    )
}
